{
  "name": "claude-vfs-memory",
  "version": "1.0.0",
  "description": "Session-scoped external virtual-file memory system: auto-offload large outputs, keyword recall, cross-session/compaction resume, context-threshold reminders, 90-day auto-cleanup. Fights memory loss and degradation on long agent tasks. Pure sh + jq, no hard dependencies.",
  "keywords": ["memory", "context", "handoff", "rollover", "vfs", "session"],
  "license": "MIT",
  "author": { "name": "skkoweio2" }
}
